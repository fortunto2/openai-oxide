# Phase 2 — Acceptance Criteria

- Embeddings: create() works, returns vector data
- Models: list(), retrieve(), delete() work
- Moderations: create() works
- Images: generate() works, supports DALL-E 3
- Audio: transcriptions.create(), speech.create(), translations.create() work
- Files: CRUD + content download works
- Multipart uploads for audio and files
- All responses strongly typed with serde
- mockito tests for every endpoint
- cargo test + clippy + fmt clean
