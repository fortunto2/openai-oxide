# Phase 3 — Acceptance Criteria

- Responses API: create() + streaming works
- Fine-tuning: jobs CRUD + events works
- Assistants: CRUD works
- Threads + Messages: CRUD works
- Runs: create, retrieve, cancel, submit_tool_outputs works
- Vector Stores: CRUD + file batches works
- Rate limit headers parsed into RateLimitInfo
- README.md with quickstart and all endpoints
- cargo doc generates clean documentation
- All tests pass, clippy clean
- Tagged v0.1.0
